<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>es.udc.agon</groupId>
    <artifactId>agon-backend</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>
    
    <name>Agon Backend</name>
	<description>Agon Backend</description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.8</version>
    </parent>
    
    <properties>
    
        <!--  Data source properties -->
        <dataSource.user>agon</dataSource.user>
        <dataSource.password>agon</dataSource.password>
        <testDataSource.user>${dataSource.user}</testDataSource.user>
        <testDataSource.password>${dataSource.password}</testDataSource.password>
    
    	<!--  Compiler options -->
    	<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>21</java.version>
        
        <!-- Package versions -->
        <jjwt.version>0.12.6</jjwt.version>
        
        <!-- Plugin versions -->
        <surefireMavenPlugin.version>3.5.2</surefireMavenPlugin.version>
        <sqlPlugin.version>3.0.0</sqlPlugin.version>
        <!--
            Permite desactivar el sql-maven-plugin (creación de tablas de test)
            fuera del entorno de desarrollo, p. ej. en el build de la imagen
            Docker de PRODUCCIÓN, donde no existe el host "db":
              mvn -DsqlPlugin.skip=true package
            En desarrollo se mantiene en false para que `mvn test` siga
            creando las tablas en la BD agon_test.
        -->
        <sqlPlugin.skip>false</sqlPlugin.skip>

        <!-- JDBC driver properties -->
        <jdbcDriver.groupId>org.postgresql</jdbcDriver.groupId>
        <jdbcDriver.artifactId>postgresql</jdbcDriver.artifactId>
        <jdbcDriver.version>42.7.2</jdbcDriver.version>
        <jdbcDriver.className>org.postgresql.Driver</jdbcDriver.className>

        <!--  Data source properties -->
        <dataSource.baseUrl>jdbc:postgresql://db/agon</dataSource.baseUrl>
        <dataSource.url>${dataSource.baseUrl}</dataSource.url>
        <!--
            Base de datos de TEST independiente: `mvn test` (sql-maven-plugin en
            process-test-classes) ejecuta 1-PostgreSQLCreateTables.sql (DROP de todas
            las tablas) y 3-SeedData.sql. Si apuntara a la BD de desarrollo 'agon'
            borraría el seed de docker-init (jornadas/encuentros/sets de 82/83).
            Al usar 'agon_test' los tests corren sobre una BD limpia y el seed de
            desarrollo permanece intacto.
        -->
        <testDataSource.url>jdbc:postgresql://db/agon_test</testDataSource.url>
        <dataSource.createTablesScript>1-PostgreSQLCreateTables.sql</dataSource.createTablesScript>
        <dataSource.createDataScript>2-PostgreSQLCreateData-init.sql</dataSource.createDataScript>
        
    </properties>
    
    <profiles>

        <!--
            Perfil para medir cobertura con JaCoCo dentro del contenedor.
            `mvn test -Pcoverage` genera backend/target/site/jacoco/index.html
            (visible en el host por el volumen ./backend:/app) y jacoco.xml.
            forkCount=1 es obligatorio: con forkCount=0 los tests corren en el
            mismo JVM de maven y el agente de JaCoCo no puede adjuntarse.
        -->
        <profile>
            <id>coverage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <version>0.8.12</version>
                        <executions>
                            <execution>
                                <id>prepare-agent</id>
                                <goals>
                                    <goal>prepare-agent</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>report</id>
                                <phase>test</phase>
                                <goals>
                                    <goal>report</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <forkCount>1</forkCount>
                            <reuseForks>true</reuseForks>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>${jdbcDriver.groupId}</groupId>
			<artifactId>${jdbcDriver.artifactId}</artifactId>
            <version>${jdbcDriver.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
		
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Google Identity Services: validación de ID Tokens de Google
             (inicio de sesión con cuenta de Google) -->
        <dependency>
            <groupId>com.google.api-client</groupId>
            <artifactId>google-api-client</artifactId>
            <version>2.7.2</version>
        </dependency>
        <dependency>
            <groupId>com.google.http-client</groupId>
            <artifactId>google-http-client-gson</artifactId>
            <version>1.44.2</version>
        </dependency>

        <!-- OpenAPI 3 / Swagger Generator -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.6.0</version>
        </dependency>
        <dependency>
            <groupId>com.sun.xml.bind</groupId>
            <artifactId>jaxb-jxc</artifactId>
            <version>4.0.5</version>
        </dependency>
    </dependencies>

    <build>
    
    	<!-- When using spring-boot-starter-parent, files under 
    	     "src/main/resources" are automatically filtered and
    	     "@propertyName@" notation is used instead of "${propertyName}". -->

        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>
    
        <plugins>
        
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefireMavenPlugin.version}</version>
                <configuration>
                    <forkCount>0</forkCount>
                </configuration>
            </plugin>

            <!--
                 - Configuration specifies onError="continue" since SQL scripts
                   try to drop tables before creating them (which causes errors
                   if such tables do not exist yet).

                 - Configuration specifies autocommit="true" since some drivers
                   (e.g. the PostgreSQL JDBC 2 driver) do not commit changes if
                   some error occurs.
            -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>sql-maven-plugin</artifactId>
                <version>${sqlPlugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>${jdbcDriver.groupId}</groupId>
                        <artifactId>${jdbcDriver.artifactId}</artifactId>
                        <version>${jdbcDriver.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <driver>${jdbcDriver.className}</driver>
                    <url>${dataSource.url}</url>
                    <username>${dataSource.user}</username>
                    <password>${dataSource.password}</password>
                    <autocommit>true</autocommit>
                    <onError>continue</onError>
                    <orderFile>ascending</orderFile>
                    <fileset>
                        <basedir>${basedir}</basedir>
                        <includes>
                            <include>
                                src/sql/${dataSource.createTablesScript}
                            </include>
                            <include>
                                src/sql/${dataSource.createDataScript}
                            </include>
                        </includes>
                    </fileset>
                    <!-- Permite desactivar la ejecución en builds sin BD de
                         desarrollo (p. ej. Dockerfile de producción):
                         mvn -DsqlPlugin.skip=true ... -->
                    <skip>${sqlPlugin.skip}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>create-tables-for-testing</id>
                        <!--
                            Bound to process-test-classes (NOT process-test-resources):
                            `mvn spring-boot:run` reaches test-compile (which includes
                            process-test-resources) but NOT process-test-classes, so the
                            dev server no longer wipes the Docker-init seed.
                            Solo se ejecuta 1-PostgreSQLCreateTables.sql: los tests crean
                            sus propios datos y el 3-SeedData.sql (DO $$ plpgsql) no se
                            puede parsear con el splitter de ';' del plugin, lo que
                            generaba un muro de errores al arrancar la suite.
                        -->
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>execute</goal>
                        </goals>
                        <configuration>
                            <driver>${jdbcDriver.className}</driver>
                            <url>${testDataSource.url}</url>
                            <username>${testDataSource.user}</username>
                            <password>${testDataSource.password}</password>
                            <autocommit>true</autocommit>
                            <onError>continue</onError>
                            <fileset>
                                <basedir>${basedir}</basedir>
                                <includes>
                                    <include>
                                        src/sql/${dataSource.createTablesScript}
                                    </include>
                                </includes>
                            </fileset>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            
        </plugins>

    </build>

</project>
